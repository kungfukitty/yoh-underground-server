{
  "version": 2,
  "builds": [
    {
      "src": "src/server.js",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",  // This regex now matches ALL incoming paths
      "dest": "src/server.js" // And directs them to your server.js
    }
  ]
}
